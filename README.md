echo "# coordinate-axes" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/sbecker11/coordinate-axes.git
git push -u origin master
# coordinate-axes
